./idea
.idea
.claude/session-log.md
.claude/settings.local.json
.claude/worktrees/

# Carries the AWS account id, hosted zone id, and cert ARN. Local only.
domain.md

# Secrets. Never commit — this repo is public.
.env
.env.*
!.env.example
.playwright-mcp/
/Boluwaji_Joshua_Adedigba_resume.pdf
/gaps.md
# macOS screenshot names contain a narrow no-break space (U+202F) before AM/PM,
# so a pattern typed with a normal space never matches. Glob past it.
/Screenshot*.png
dev/
