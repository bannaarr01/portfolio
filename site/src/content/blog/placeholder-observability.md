---
title: 'PLACEHOLDER — What a Dashboard Cannot Tell You'
description: Disposable placeholder post. Group 07 deletes this. Here so collection queries return non-empty during Wave 2.
category: system-design
publishDate: 2026-08-22
draft: false
tags: ['placeholder', 'observability']
heroGlyph: activity
---

**This post is a placeholder.** Group 07 owns `src/content/**` and replaces it.
It exists so `getPublishedPosts()` returns something while Wave 2 builds the
templates that consume it.

## A heading, so the TOC has something to render

Body copy long enough that `minutesRead` computes to something other than the
floor of one minute. A dashboard shows you what you thought to measure, which
is a different thing from what is actually wrong. The gap between those two is
where most incidents live.

### A nested heading, so depth handling gets exercised

```go
func main() {
	// A fenced block, so the dual-theme Shiki config gets exercised too.
	fmt.Println("placeholder")
}
```

- A list item
- A second list item
- A third, so list spacing is visible

> A blockquote, because the prose styles in group 05 will need one.
