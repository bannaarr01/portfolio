---
title: 'PLACEHOLDER — A Draft That Must Never Ship'
description: Disposable placeholder. draft:true, so its absence from every query, the sitemap, and dist/ is the proof the filter works.
category: go
publishDate: 2026-08-25
draft: true
tags: ['placeholder']
---

**This post is a placeholder, and it is a draft.**

If this text ever appears in `dist/`, in the sitemap, or in a rendered listing,
something bypassed `getPublishedPosts()` and queried the collection directly.
That is the failure this file exists to catch.
