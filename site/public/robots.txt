# Group 06 (seo-404-feeds) owns the content of this file; group 07 owns the
# path it lives at. This is a working default so the route resolves during
# development. If 06 ships different directives, theirs win.
#
# The Sitemap line must be an absolute URL. @astrojs/sitemap emits
# /sitemap-index.xml at the site root, driven by `site` in astro.config.mjs.
# TODO: replace the host below with the real production domain.

User-agent: *
Allow: /

# Nothing under /og/ is useful to a crawler on its own; the images are
# referenced from page metadata.
Disallow: /og/

Sitemap: https://TODO-your-domain.example/sitemap-index.xml
